node_modules/
out/
dist/
*.log
.DS_Store
.env
.env.*
*.local
coverage/

# Working docs, briefs and reports — local notes, not shipped artifacts.
# Files already tracked stay tracked; this stops NEW ones being added.
docs/
release/
registry/data/

# Probe scripts, QA drivers and one-off harnesses — useful locally,
# noise in history. Kept on disk, out of the repo.
scratchpad/
registry/assets/site/raw/

# Perf gate measurements (tests/perf, COOKREW_PERF_REPORT) — CI uploads them as artifacts.
perf-results/
