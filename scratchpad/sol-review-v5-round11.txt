[P0] The input-provenance WAL fails open on the failures a WAL exists to survive
WHERE: src/main/input-provenance.ts:116-143,168-219
CLAIM: A pane write is allowed even when its write-ahead fact did not durably land, and an unreadable WAL is treated as proof that every surviving pane is clean.
EVIDENCE: markDirty mutates records before persist(), but persist catches every mkdir/write/rename error and returns success to the caller; the subsequent PTY write therefore proceeds, and records.has prevents any retry in this process. The temp file is never fsynced and the parent directory is never fsynced, so a crash can lose a first mark after rename. loadRecords also maps every read error to an empty map silently and maps parse/schema corruption to an empty map after logging. Each path creates the forbidden false-clean restart while the backing pane can still contain owner or dispatch bytes.
FIX: Make mark/upgrade return only after an atomic durable write (full write, file fsync, rename, parent-directory fsync), preserve/retry failed state, and refuse the pane write when the mark cannot commit. Distinguish ENOENT first-run from read/corruption faults and adopt all live panes unknown/dirty on the latter.

[P0] Local input observation is not proof that an asynchronous PTY write was consumed
WHERE: src/main/input-provenance.ts:63-82; src/main/pty.ts:442-474,496-518; src/main/turn-tracker.ts:1538-1570,1695-1705; node_modules/node-pty/src/unixTerminal.ts:330-374
CLAIM: The WAL can be skipped or cleared before the bytes reach the persistent pane, reopening a crash window even with a perfectly durable provenance file.
EVIDENCE: dirtiesInputBox declares a self-contained chunk such as "abc\r" clean. PtySession then calls proc.write and immediately emits its own input event; that event makes TurnTracker clearOwnerEditing on the modeled submit. node-pty's Unix write() explicitly queues an asynchronous fs.write, handles partial writes by retaining an offset, and may defer the remainder after EAGAIN. Thus the app can die, or the write can fail, after only "abc" crossed while no WAL fact exists, or after a prior dirty fact was already cleared by the locally emitted CR. The comments call this an observed submit, but it is only an enqueue observation, not a pane-consumption acknowledgement.
FIX: Mark before every input-bearing write including submit-containing chunks, and clear only on a positive backend/write-completion witness that the entire submit crossed and consumed the shared box (or on a proven fresh-pane restart). Do not use the synchronous proc.write return or the locally emitted input event as that witness; add partial-write/crash tests against an injectable acknowledged writer.

[P0] A crash-stranded dispatch paste is adopted as owner editing and owner submission is allowed
WHERE: src/main/input-provenance.ts:35-43; src/main/producer-lease.ts:361-468; src/main/ask.ts:78-115,382-404; src/main/turn-tracker.ts:1760-1772; tests/input-provenance.test.ts:196-220,266-280
CLAIM: The WAL loses the producer identity during the exact paste-before-CR crash window, so restart protection is asymmetric: another dispatch is blocked, but an owner Enter can submit the stranded dispatch brief.
EVIDENCE: pasteAndSubmit and writeFromDispatch initially persist only generic dirty; contamination is upgraded only after the delay returns and stillValid is observed false. A process death immediately after the paste never reaches that upgrade. On restart adopt maps dirty to ownerEditing, not contaminated. acquireOwnerLease checks contamination and the dispatch guard, but intentionally does not reject ownerEditing, while guardOwnerInput likewise rejects submits only for contaminated residue or an armed/held dispatch. Therefore the owner can submit beside the dead dispatch's text. The tests cover graceful cancellation after the timer and completed paste+CR, not death immediately after the first write.
FIX: Persist a producer-qualified delivery-in-progress/dispatch-residue fact before a dispatch paste crosses. It must block every submit-capable producer after adoption and may clear only after acknowledged CR consumption or a proven fresh pane; upgrade uncertainty to contamination during shutdown/recovery.

[P0] Cancellation can cross the new awaited agent-resolution gap and still launch a prompt
WHERE: src/main/dispatch.ts:1578-1647,1749-1768; src/main/herdr-host-multiplexer.ts:1223-1235,1445-1491; src/main/herdr-agent-wait.ts:237-266
CLAIM: The async conversion inserted an await after the dispatch engine's last liveness check but before the irreversible submit, so an interrupted dispatch or retired owner ask can still reach the prompt CLI.
EVIDENCE: DispatchService checks deliveryLive, creates the AbortController, then awaits mux.submitAgent. submitAgent awaits agentResolvableAsync, whose asyncRunner accepts no AbortSignal. If interrupt/release/retire fires during that lookup, the controller becomes aborted but the lookup continues; after it resolves true, submitAgent calls submitViaHerdr without rechecking signal.aborted. runCli always spawns execFile first and only then notices a pre-aborted signal and sends SIGTERM, leaving a real spawn-to-kill race in which herdr can submit the prompt. nativeAsk uses the same mux path. The dispatch then sees deliveryLive false and records no delivery even though the bytes may have crossed.
FIX: Thread AbortSignal through agent resolution, refuse before spawning when already aborted, and revalidate signal/generation/deliveryLive after every awaited preflight in the same synchronous stretch as submission. Add cancellation-during-agent-get tests proving the prompt child is never spawned.

[P1] Permanent retirement clears provenance before pane death is proven
WHERE: src/main/producer-lease.ts:242-295; src/main/index.ts:955-976,994-1019,1191-1221,1358-1377; src/main/pty.ts:1035-1081
CLAIM: retire() treats the pane as gone and deletes its durable input fact before callers attempt a mostly best-effort kill; a surviving pane can later be recovered with a false-clean box.
EVIDENCE: retireTerminal calls ProducerLease.retire first, and retireOne immediately provenance.clear's. removeNode, workspace deletion and removeCut kill afterward through kill/killDetached, which do not wait or surface backend refusal. Even the CWD/cut killAndWait path clears first and can then throw because the session survived. A recover/rebind after that failure can adopt the same persistent pane, but the WAL fact protecting its old buffer is already gone.
FIX: Split logical generation retirement from durable clean proof. Preserve the provenance fact while kill is pending; clear it only after killAndWait positively proves pane death, and keep/upgrade it fail-closed on any kill failure or rollback.

[P1] App shutdown cancellation covers only one ask phase and is a one-shot snapshot
WHERE: src/main/ask.ts:123-216,251-336,536-592; src/main/index.ts:1938-1970; src/main/mobile-server.ts:487-509
CLAIM: before-quit can still leave an owner ask's herdr wait child alive, and it has no admission latch preventing a new ask after the cancellation snapshot.
EVIDENCE: only nativeAsk registers in activeAsks. When submitAgent returns failed, nativeAsk unregisters and askTerminal falls through to ownerSubmit followed by waitForReply with no AbortSignal; waitUntilIdle can then run a ten-minute herdr child invisible to cancelAllAsks. cancelAllAsks snapshots the Set once and no shutdown flag rejects later registration. before-quit does not close the mobile/socket admission surfaces before that snapshot, so a request already awaiting its body/session can enter after it. retireAll does not make the lease permanently closed; a new ask simply captures the new generation.
FIX: Register one cancellation scope around the entire ask, including typed fallback, waitUntilIdle, grace and quiescence. Add a shutdown latch that rejects new asks before draining, close producer admission first, and drain until the registry is empty within the bounded TERM-to-KILL window.

[P1] Async herdr capture and report children have no owner at shutdown
WHERE: src/main/herdr-host-multiplexer.ts:284-309,578-613,1274-1312,1374-1435; src/main/dispatch.ts:1503-1547,1604-1645; src/main/index.ts:1941-1970
CLAIM: Moving dispatch reads and reports off the main thread fixed synchronous latency but created untracked CLI children that interruptAll, retireAll and cancelAllAsks cannot abort or await.
EVIDENCE: AsyncCliRunner has no AbortSignal or child registry and unrefs every execFile. Dispatch creates its delivery AbortController only after the context and deep captures; reportAgentSession is fire-and-forget and may keep retrying/spawning too. before-quit interrupts dispatch and awaits active owner asks only, so a 5-second pane read, 3-second agent/report operation, or a retry admitted before teardown can survive the final app.quit and run against stale state. Its own timeout is a bound, not ownership by the process that commissioned it.
FIX: Give AsyncCliRunner an AbortSignal and a tracked child registry, create the delivery controller before the first capture, cancel every capture/report/refresh on retirement or shutdown, and await one bounded cancelAllHerdrOperations gate before app.quit.

[P1] The fold still has an unbounded synchronous record unit
WHERE: src/main/turn-store.ts:67-82,1078-1149,1167-1226; tests/turn-store-fold-bytes.test.ts:112-204
CLAIM: Byte-sized surrounding chunks do not bound main-thread work when one TurnRecord is arbitrarily large, so round 10's tail-latency finding remains for exactly the worst record.
EVIDENCE: applyRegion synchronously converts, splits and JSON.parse's an entire oversized line; replaceChunked synchronously this.line/JSON.stringify's and Buffer.from's one oversized record. Yields before and after isolate the stall but cannot shorten it. There is no record-size ceiling, and the gate explicitly accepts this residual while using an 8 MB fixture and only requires best-of-three max gap below 800 ms, far above interactive tail budgets. A large tool reply can therefore monopolize Electron main in one unbounded stretch.
FIX: Parse/serialize folds in a worker (or enforce a hard, safely truncated record-size bound before storage) and gate maximum event-loop delay at the product latency budget, including one maximum-size record.

[P1] flushAll can miss directory debt created by an already-running fold
WHERE: src/main/turn-store.ts:995-1037,1167-1285,1347-1369; src/main/turn-tracker.ts:1396-1399; src/main/index.ts:1956-1969
CLAIM: The shutdown debt sweep is point-in-time and does not await folds, so a fold can rename after flushAll inspected dirDebt and create a new unsatisfied durability obligation during shutdown.
EVIDENCE: foldNow is fire-and-forget and absent from flushAll's contract. A fold can be awaiting async temp writes/fsync when TurnTracker.flushHistories calls flushAll and sees no debt. It can then pass its generation check, rename, add dirDebt, and fail the async parent-directory fsync. The retry timer is unref'd and there is no second flush before browser shutdown completes and app.quit runs. The folded file is logically published but its directory entry is unproven at the exact crash boundary the debt mechanism claims to close.
FIX: Track fold promises and make shutdown an async bounded drain: stop scheduling, await/abort in-flight folds, then settle every debt created through their completion before final quit. Add the interleaving test: fold paused before rename -> flushAll starts -> rename -> dir-fsync failure.

[P1] Concurrent transcript-binding reports can restore an obsolete rotated path
WHERE: src/main/herdr-host-multiplexer.ts:1374-1435; src/main/index.ts:663-676,704-715
CLAIM: The async single-flight key includes the path, so it prevents duplicate reports but permits old-path and successor-path reports for one pane to race and publish in reverse order.
EVIDENCE: reportAgentSession("pane", oldPath) and the rotation-triggered reportAgentSession("pane", newPath) occupy different Set keys and run independent CLI children. If the newer command lands first and the older one completes last, herdr is rebound to the predecessor transcript indefinitely; no per-pane revision or latest-path check suppresses the stale completion. That can make lifecycle detection and native ask waiting observe the wrong conversation after rotation even though Cookrew's own SessionTurnSync correctly migrated.
FIX: Serialize/coalesce reports per pane with a monotonic binding revision: only the latest requested path may publish, and a successor request must cancel or supersede the predecessor. Test delayed-old/fast-new completion order.

[P1] Owner reply-wait still forks a synchronous pane inventory on Electron main
WHERE: src/main/ask.ts:536-561; src/main/herdr-host-multiplexer.ts:529-550,691-696,1494-1511
CLAIM: The dispatch path is now zero-sync, but each acknowledged owner ask can still run a synchronous herdr pane-list process before its asynchronous wait, serializing concurrent asks on the main thread.
EVIDENCE: waitForReply calls mux.waitUntilIdle. HerdrHostMultiplexer.waitUntilIdle resolves the pane with paneFor, paneFor calls panes, and outside an attach batch panes calls readPanes -> this.herdr -> runner.run/execFileSync. The signal is not consulted until after this synchronous chain. Concurrent owner asks therefore recreate a per-ask process storm in a user-facing IPC/socket path and cannot be cancelled while that lookup blocks.
FIX: Resolve waitUntilIdle from the same bounded inventory as submitAgent, or use one async inventory lookup carrying the ask signal; add a structural no-sync-runner gate through submission acknowledgement and reply-wait entry for concurrent asks.

R10-#1: PARTIAL - Happy-path dirty/contaminated adoption exists, but WAL storage fails open, local enqueue is mistaken for consumption, dispatch residue loses producer identity, and retirement clears before pane death.
R10-#2: FIXED - Any bracketed-paste marker now makes opacity sticky through completed single-line and split-close cases until observed submit or retirement.
R10-#3: FIXED - Dispatch admission, transcript reporting, capture/deep-capture, registry resolution and submission use the async runner with the structural zero-sync gate; the new cancellation and child-ownership failures are separate findings above.
R10-#4: PARTIAL - Native asks are registered and bounded at quit, but typed-fallback reply waits, post-snapshot asks and other async herdr children remain outside the drain.
R10-#5: FIXED - Store-load seeding covers attached and detached pre-existing nodes, and workspace removal, node removal and both cut legs pair retirement with forgetTerminal.
R10-#6: PARTIAL - Ordinary fold units are byte-bounded and durable I/O is asynchronous, but one unbounded TurnRecord still parses and serializes synchronously on Electron main.
R10-#7: PARTIAL - Fold-created debt now retries and a quiet standing debt is included in flushAll, but shutdown can miss debt created afterward by an in-flight fold.

VERDICT: UNSOUND - P0:4 P1:7 P2:0
